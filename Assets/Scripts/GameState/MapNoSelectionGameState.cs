using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Assets.Scripts.GameState
{
    class MapNoSelectionGameState : AbstractMapGameState
    {
        public override void HandleAccept()
        {
//            GameManager.instance.levelManager.GetSelectedTile();


            // Check if selection is null. If so, return

            // else if the selection is a friendly unit interface, open a menu

            // Else if enemy unit interface,
        }
    }
}
